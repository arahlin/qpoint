This directory contains a complete and unedited version of the IAU SOFA library, issue 2017-04-20, available from http://www.iausofa.org/

The original `00READ.ME` and the original `makefile` (renamed to `makefile.orig`) are enclosed.  The only change in this directory is the inclusion of a `qpoint`-compatible `Makefile`.
